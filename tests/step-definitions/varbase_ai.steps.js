'use strict';

/**
 * @file
 * Custom step definitions for the Varbase AI test suite.
 *
 * The suite reuses the step definitions that ship with varbase-e2e (navigation,
 * web-first assertions, accessibility). The only module-specific step is
 * logging in as a named user from cucumber.js worldParameters.users (varbase-e2e
 * does not ship a Drupal form-login step).
 */



